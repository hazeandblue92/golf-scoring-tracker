/**
 * Scoring control room (spec §5.2): group progress, missing scores,
 * conflicts, stale devices, correction requests, projection health,
 * open/close controls, and finalization blockers.
 */
export function AdminEventScoring() {
  return (
    <>
      <h1>Scoring control room</h1>
      <p>
        Group progress, missing scores, conflicts, projection health, and
        finalization controls for this event.
      </p>
    </>
  );
}
