/**
 * Settings (spec §5.1 /settings): account, device, and offline-data
 * preferences.
 */
export function Settings() {
  return (
    <>
      <h1>Settings</h1>
      <p>Account, device, and offline data preferences.</p>
    </>
  );
}
